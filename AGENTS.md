# Workspace Notes

- Python executable: `C:\Users\alano\anaconda3\python.exe`
- A private local package is installed in `.venv`. It may be used locally, but must never be committed, copied into tracked files, published, uploaded, or otherwise exposed publicly. Do not record its name, version, artifact path, source location, or contents in this public repository.
